# vs-ai2.38: robust space — space that survives the worst cut

## Overview

The bot opens from an empty board with a width-1 diagonal tendril
(10,10/9,9/8,8 …) that races to the center. Every cell of that filament is an
articulation point, so one cut by the opponent forfeits the whole distal chain
and the game. The space-race eval term (`spaceRaceWeight=32`, `evaluate.go`)
pays ~222/cell for deepest-first Voronoi expansion; the diagonal is
Voronoi-optimal, so the eval loves it and nothing prices its fragility in the
opening.

Attempt 1 (a competing ungated own-cutLoss PENALTY term) was a null result: no
weight flips the opening without wrecking strength (see the in-code note at the
top of `evaluate.go` and `docs/plans/completed/20260717-vs-ai2.38-ungated-fragility.md`).
DO NOT retry a penalty term.

**This attempt — ROBUST SPACE: change what the space term COUNTS, not add a
rival term.** Seed each player's space-race BFS not from their full connected
territory, but only from the component that SURVIVES their own worst single-cell
cut. Concretely: find the player's max-`cutLoss` articulation cell (already
computed in `articulationPointsInto` → `workspace.cutLoss`), hypothetically
remove it, and seed the Voronoi partition only from the surviving,
base-containing component. A width-1 tendril then collapses to ~the base and
claims ~zero deep space; a width-2 front has no single cut point so it keeps its
full body and full space credit. Same units (cells), NO new weight to tune —
`spaceRaceWeight` stays 32. Applied symmetrically to every active player.

## Context (from discovery)

- **Only file to change for the fix:** `backend/search/evaluate.go`.
  - `spaceRace(state, cells, connected, w)` (line ~87) runs one shared
    multi-source BFS seeded from every player's `connected[p]` mask. Swap the
    seed masks; the BFS body is unchanged.
  - `articulationPointsInto` (line ~450) roots its DFS at the player's base, so
    `cutLoss[i]` = number of own cells that disconnect from the base if cell `i`
    is captured, and it is nonzero only for own `Normal` articulation cells (the
    base itself is always zeroed at lines ~500-511). Max `cutLoss` = worst cut.
  - `connected[player-1]` (from `allConnectedInto`) is the base-connected mask
    (BFS from base over own cells) — already exactly one component.
  - `evalWorkspace` (line ~42) owns all reusable buffers; `ensure(size)` sizes
    them. `w.queue` is free after `allConnectedInto`; `spaceRace` borrows it.
  - `evaluateAllWithWorkspace` (line ~159): currently calls `spaceRace` at
    line ~177 BEFORE the per-player `analyzeWithConnectivity` loop. This must be
    reordered — the robust seed needs `cutLoss`/`articulation` from the metrics,
    which are computed by `analyzeWithConnectivity`.
- **Frozen — DO NOT TOUCH:** `backend/search/incumbent/**` (byte-frozen
  baseline), `backend/search/search.go`, `backend/game/**`.
- **Goldens that WILL move (re-pin honestly, values only):**
  - `backend/search/evaluate_test.go` — `TestEvaluateWorkspaceGoldenStates`
    (fixture `.want` score vectors, currently labeled "vs-ai2.34 golden").
  - `backend/search/search_test.go` — fixed-depth / fixed-node golden fixtures
    (`wantDepth` / `wantNodes`).
- **Gates (env-gated tests in `backend/arena`):**
  - `TestConstructedCutWidthSensitivity` (`VS_CONSTRUCTED_CUT=1`,
    `constructed_cut_gate_test.go`): asserts width-1 cutter WINS (fragility) and
    width-2 cutter LOSES (invariant). BEFORE (measured on this base): width-1
    candidate cutter WINS in 27, width-2 candidate cutter loses in 35. The
    width-1 row is the PRIMARY FLIP target.
  - `TestBotOpeningReplyInvariance` (`VS_EMPTY=1`, `empty_opening_gate_test.go`):
    reports the bot's first reply per opening. BEFORE: width-1 diagonal
    `[{8 8} {9 9} {10 10}]`, invariant across all 8 openings.
  - `TestLadderReport` (`VS_LADDER=1`, slow) + small-board `TestStrengthGate`:
    no-regression.

## Development Approach

- Testing approach: Regular. The "tests" here are the pre-existing gate battery
  and the golden fixtures — do NOT invent new unit tests; re-pin existing
  goldens honestly (recompute, paste the new values, keep the comparison logic).
- Keep the eval allocation-free: reuse workspace buffers, no per-eval `make`.
- Complete each task fully; run the relevant build/test before the next.
- If the primary flip does NOT happen (width-1 cutter still wins AND opening
  reply still width-1), this is another null result: revert the eval change,
  restore the original goldens, and record the ship-nothing outcome. Do not
  keep a strength regression for no flip.

## Testing Strategy

- Unit/golden: `go test ./search` (re-pinned goldens must pass).
- Gates: `VS_CONSTRUCTED_CUT=1` and `VS_EMPTY=1` arena tests (fast) — capture
  before/after; the slow `VS_LADDER` no-regression pass is run by the executor
  outside ralphex.
- Race: `go test -race ./search ./game ./arena ./cmd/bot-hoster`.

## Progress Tracking

- Mark completed items `[x]` immediately when done.
- Add newly discovered tasks with ➕ prefix; blockers with ⚠️ prefix.
- Keep this plan in sync with actual work.

## Implementation Steps

### Task 1: Add robust-space reseed to the eval

- [x] In `evalWorkspace` (`backend/search/evaluate.go`) add a per-player seed
      buffer `spaceSeed [4][]bool`; size it in `ensure(size)` next to
      `articulation`/`cutLoss` (`w.spaceSeed[i] = resize(w.spaceSeed[i], size)`).
- [x] Add function `robustSpaceSeed(state, cells, player, connected []bool,
      articulation []bool, cutLoss []uint16, queue []int, seed []bool)`:
      (1) find the index of the max-`cutLoss` cell among `articulation` cells
      (`cut`, -1 if none); (2) `clear(seed)`; (3) BFS from the player's base
      over cells where `connected[ni]` is true, SKIPPING `ni == cut`, marking
      `seed[ni]=true`. This yields the base-containing component that survives
      removing the single worst cut. If base is not connected, leave `seed`
      empty. Model the BFS on `connectedCellsInto` (same base lookup,
      `neighbors`, `queue` walk) — reuse `basePos`/`indexFor`/`neighbors`.
- [x] In `evaluateAllWithWorkspace`: REMOVE the early `space := spaceRace(...)`
      call (line ~177). Split the current player loop: first compute
      `metrics[index] = analyzeWithConnectivity(...)` for every active player
      (this fills `cutLoss`/`articulation`); then, in a second loop, for each
      active player call `robustSpaceSeed(...)` into `workspace.spaceSeed[index]`
      and for each INACTIVE player `clear(workspace.spaceSeed[index])`; then
      `space := spaceRace(state, cells, workspace.spaceSeed, workspace)`; then
      the existing `raw[...] = ...` scoring loop (unchanged body, still reads
      `space[player-1]`). `robustSpaceSeed` may use `workspace.queue` (free at
      that point; `spaceRace` re-borrows it afterward).
- [x] Replace the attempt-1 null-result comment block at the top of
      `evaluate.go` (lines ~12-18) with a short note that the fragility PENALTY
      was replaced by robust space: the space-race seed is the post-worst-cut
      surviving component (see this plan), so a width-1 tendril simply stops
      paying — no weight to tune. Keep `spaceRaceWeight=32`.
- [x] `cd backend && go build ./... && go vet ./search` — must pass.

### Task 2: Re-pin production-eval goldens honestly

- [ ] `cd backend && go test ./search` — capture the golden failures
      (`TestEvaluateWorkspaceGoldenStates`, and any fixed-depth/fixed-node
      goldens in `search_test.go`).
- [ ] Update the `.want` score vectors in `evaluate_test.go` and the
      `wantDepth`/`wantNodes` fixtures in `search_test.go` to the newly computed
      values (paste the actual `got`). Update the "vs-ai2.34 golden" label text
      to "vs-ai2.38 robust-space golden". Do NOT change the comparison logic or
      loosen any assertion — values only.
- [ ] `cd backend && go test ./search` — all green.

### Task 3: Run the primary-flip gates and re-pin their expectations

- [ ] `cd backend && VS_CONSTRUCTED_CUT=1 go test ./arena
      -run TestConstructedCutWidthSensitivity -v` — record width-1 and width-2
      candidate outcomes (decisions + winner).
- [ ] `cd backend && VS_EMPTY=1 go test ./arena
      -run TestBotOpeningReplyInvariance -v` — record the bot reply cells per
      opening and whether they are still width-1.
- [ ] IF width-1 flipped (cutter no longer wins) AND width-2 still loses:
      update the `TestConstructedCutWidthSensitivity` width-1 case to
      `{1, false, "robust space widens the opening — filament no longer
      severable"}`, keeping width-2 `{2, false, ...}`. Confirm the test passes.
- [ ] IF the opening reply is no longer the invariant width-1 diagonal, that is
      the intended VS_EMPTY outcome — the invariance report is a log, not an
      assertion, so no code change is required there; just record the new table.
- [ ] IF NEITHER flipped: this is a null result — `git checkout` the eval and
      golden changes (Tasks 1-2), and record ship-nothing in this plan's
      Post-Completion. Skip remaining verification.

### Task 4: Verify build/vet/test/race

- [ ] `cd backend && go build ./... && go vet ./...` — clean.
- [ ] `cd backend && go test ./search ./game ./arena ./cmd/bot-hoster` — green.
- [ ] `cd backend && go test -race ./search ./game ./arena ./cmd/bot-hoster` —
      green.

## Technical Details

- `cutLoss[i]` semantics: DFS is rooted at the base, so subtree sizes measure
  cells separated FROM the base when `i` is removed — exactly the loss the
  robust seed must exclude. Max over articulation cells = worst single cut.
- Symmetry: every active player's seed is shrunk the same way from the same
  position in the same shared BFS, so `spaceRace`'s side-to-move-independence
  and tie/contested handling are preserved (opponent fragile space is also
  discounted, which aligns with the existing predatory cut bonus at lines
  ~212-228).
- Cost: one extra base-BFS per active player per eval (O(cells)); affordable
  under the 1000ms budget after the vs-ai2.36 Position-path speedups.

## Post-Completion

**Executor-run (outside ralphex):** slow no-regression battery —
`VS_LADDER=1 ... TestLadderReport` (reduced openings for signal), small-board
default `TestStrengthGate` (greedy>=75%/legacy>=85%), from-empty vs naive
cutters must not collapse. Report before/after tables honestly; ship nothing if
the flip failed. Draft PR only, base main; do not merge.
